# App theme — colors taken from the alarm-clock logo.
[theme]
primaryColor = "#F5391F"          # red accent: buttons, sliders, active tabs
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F2F4F7"  # neutral light gray for input fields
textColor = "#16256C"             # navy, matches the logo background
